import type { AppManifest } from '../_types';

export const manifest: AppManifest = {
  id: 'applyTemplate',
  title: 'Vibecode - Apply Commit Lint Template (internal)',
  description: 'Internal command fired by tree-view template nodes; skips the QuickPick and applies the chosen template directly.',
  menus: [],
  palette: false
};
