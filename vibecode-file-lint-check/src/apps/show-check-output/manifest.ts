import type { AppManifest } from '../_types';

export const manifest: AppManifest = {
  id: 'showCheckOutput',
  title: 'Vibecode - Show File-Lint Check Output',
  description: 'Focus the shared output channel (internal — bound to tree node click).',
  menus: [],
  palette: false
};
